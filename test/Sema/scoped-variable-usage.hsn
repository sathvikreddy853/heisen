{
    let x: int = 2;
}

# let x: int = 3;

func foo () : int {
    # let x: int = 2;
    let y: int = x + 2;
    return x + y;
}

func bar () : int {
    let x: int[4] = {1, 2, 3, 4};
    let sum: int = 0;
    for (i: int = 0; i < 4; i += 1) {
        sum += x[i];
    }

    return sum;
}