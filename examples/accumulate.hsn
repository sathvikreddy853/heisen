func accumulate (arr: int[]): int {
    let n: int = arr.size;

    let sum: int = 0;
    for (i: int = 0; i < n; i += 1) {
        sum += arr[i];
    }

    return sum;
}

let arr: int[] = [1, 2, 3, 4, 5];
let sum: int = accumulate (arr);
print (f"Sum: {sum}");
