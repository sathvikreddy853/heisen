func fib (n: int) : int {
    if (n <= 1) {
        return n;
    } else {
        return fib (n - 1) + fib (n - 2);
    }
}

let n: int = 10;
let result: int = fib (n);
print (f"Fib({n}) = {result}");
