
func bernstein_vazirani (n: int, Uf: (qubit[], qubit) => ()) : bit[] {
    let res: bit[n];
    let q: qubit[n + 1]

    let x = q[0:n];
    let y = q[n];

    H(n) @ x;
    H @ X @ y;

    Uf(x, y);

    H(n) @ x;

    measure x => res;
    return res;
}
